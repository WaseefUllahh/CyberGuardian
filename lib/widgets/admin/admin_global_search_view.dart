import 'package:flutter/material.dart';

class AdminGlobalSearchView extends StatelessWidget {
  const AdminGlobalSearchView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Global Search View Placeholder'));
  }
}
